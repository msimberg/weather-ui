services:
  weather-ui:
    build: .
    ports:
      - "${PORT:-8087}:8087"
    environment:
      PIRATE_WEATHER_API_KEY: ${PIRATE_WEATHER_API_KEY:?PIRATE_WEATHER_API_KEY must be set}
      RUST_LOG: weather_ui=info,tower_http=info
    restart: unless-stopped
